services:
  bank:
    build: .
    ports: ["3001:3001"]
    environment:
      PORT: "3001"
      HOST: "0.0.0.0"
    volumes:
      # Named volume — SQLite WAL mode is unreliable on Windows DrvFs bind mounts.
      - bank-data:/app/data

volumes:
  bank-data:
